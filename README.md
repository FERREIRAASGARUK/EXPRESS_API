# Listar-estados-back
Este é o repositório que contém uma api em express feita por mim para prover ao front (Listar estados front) uma lista de estados para serem mostrados na tela do usuário.
# INICIALIZAR: 
yarn start
